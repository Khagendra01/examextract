services:
  - type: web
    name: examextract
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_exam:app --bind 0.0.0.0:$PORT --timeout 300
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
